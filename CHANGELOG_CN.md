# 更新日志 (Changelog)

本项目的所有重要变更均记录于此文件中。
文档格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，并严格遵循 [语义化版本规范](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]

## [0.1.6] - 2026-09-10

### 新增 (Added)
- **自定义窗口标题栏**:
  - 基于 egui 渲染的自绘无边框窗口标题栏，高度与底部工具栏严格统一为 `26.0px`。
  - 居中展示窗口标题与当前页面名称，支持标题栏区域拖拽移动窗口与双击最大化/还原切换。
  - 纯矢量绘制的窗口控制按钮（最小化、最大化/还原、关闭及悬停高亮），保证高 DPI 屏幕下的锐利渲染，无需依赖额外字形图标。
  - 完整的 8 方向边缘与四角无边框拖拽缩放手柄。
  - 窗口四周带有 1 像素微弱边框，提升无边框窗口的辨识度与层次感。
- **状态提示区域**:
  - 动态状态栏统一显示未加载引导、当前页面与视频流元数据、2 秒操作瞬态反馈以及悬停热区目标指示。

### 变更 (Changed)
- **底部控制栏布局与播放器控件**:
  - 底部控制面板重构为清晰的双行布局：第 1 行专用于光盘加载管理与常驻视频播放控制，第 2 行用于导航、调试开关与状态信息展示。
  - 视频播放控制按钮更新为精致的图标控件（`▶`/`⏸`, `⏹`），支持悬停快捷键提示（`Space`, `ESC`）。
  - 进度条自动拉伸填充剩余可用宽度，右侧显示已播放时间与总时长。
  - 统一底部面板上下边距与分割线间距为 `6.0px`。
  - 统一工具栏各元素与垂直分割线之间的水平间距为 `8.0px`。

### 修复 (Fixed)
- **交互脚本动画显示延时**:
  - 在脚本虚拟执行机中为 `DRAWIMAGE` 指令加入标准的 300ms 显示延时让出机制，使多帧交互动画（如射击打击序列、五角星计分累加动效）能够平滑呈现，并与音效时长精确同步。
  - 在动画或延时执行期间禁用鼠标热区悬停与点击交互，防止连贯动画被打断。
  - 优化主循环画布纹理刷新逻辑，支持就地增量更新，消除不必要的 GPU 纹理句柄重新分配开销。
- **热区悬停残留清理**:
  - 点击 DAT 视频跳转目标及视频播放期间，立即清除遗留的热区悬停高亮状态。
- **无头测试环境音频初始化异常**:
  - 在无头持续集成环境（`CI=true` / `GITHUB_ACTIONS=true`）下自动绕过底层原生音频驱动初始化，防止因缺少硬件音频驱动导致的 `0xc0000005` 内存访问异常崩溃。

---

## [0.1.5] - 2026-09-08

### 新增 (Added)
- **复合页面覆盖层贴图支持**:
  - 支持解析复合 CHM 页面并将覆盖图层精确合成到底层画布（例如习题页面的答案补丁图 `AS011_*.YBM`）。
  - 在覆盖层文档未占用热区区域时，完整保留底层背景与基础页面的热区点击交互能力。
  - 页面导航跳转时自动清理覆盖层状态。
- **NTSC 视频流解码支持**:
  - 扩展 MPEG-1 视频解码器，除了标准的 PAL 制式（`352x288` @ 25.0 fps）外，新增对 NTSC 分辨率（`352x240` @ ~29.97/30.0 fps）视频流的自适应解码与播放。

### 修复 (Fixed)
- **下划线热区点击容差优化**:
  - 为细窄下划线热区（如填空题横线）增加向上点击容差，允许用户自然点击横线上方的空白区域即可触发交互。
- **视频播放并发与 ESC 退出防死锁**:
  - 修复视频播放期间按下 Escape 键时可能引发的锁竞争与死锁问题，确保瞬时平滑返回上一个 CHM 页面。

---

## [0.1.4] - 2026-09-07

### 新增 (Added)
- **动态文本变量与计分展示 (Chunk 14 `TEXT`)**:
  - 支持解析并渲染 Chunk 14 动态文本覆盖层到画布。
  - 实现变量实时格式化与计分器展示（例如互动游戏页面 `GAME051` 和 `SCORE05X` 中的得分统计）。
- **热区微脚本支持 (Chunk 13 `MICRO`)**:
  - 支持执行嵌入在热区记录中的单行微脚本（例如变量自增与赋值 `i_e=i_e+2`）。

### 修复 (Fixed)
- **片头视频播放条件约束**:
  - 严格依据 `AUTORUN.CLS` 中是否存在 `PlayMpeg` 符号或显式 `.DAT` 路径触发开场视频，避免在无片头视频的光盘上意外播报静音或警告音轨。
- **CHM 数据缓冲与脚本注释容错**:
  - 优先读取 CHM Chunk 3 偏移 `0x60` 处的定长前缀图像文件名，防止脏数据干扰。
  - 在拆分脚本指令时，完整保留 `REM` 与 `'` 注释内容中的冒号符号。
  - 异步后台脚本运行完毕后不再强制跳转，确保交互页面持续展示。

### 变更 (Changed)
- 启动时默认折叠虚拟遥控器面板，提供更整洁纯净的观赏体验。
- 完善项目说明文档，增加预编译二进制安装与开发指南。

---

## [0.1.0] - 2026-09-06

### 新增 (Added)
- **光盘格式解析引擎**:
  - 支持解析光盘根目录类描述文件（`AUTORUN.CLS` / `ENREACH.CLS`）并准确定位启动首页。
  - 支持解析编译型 `<COMPHTML>` (.CHM) 页面，包括背景图像引用、页面导航跳转表与多边形/矩形按钮热区。
  - 支持解码 `<YUVBMP>` (.YBM) 8 位 YUV 调色板图像并转换为 RGBA。
- **VCDSCRIPT 脚本引擎**:
  - 支持词法分析与抽象语法树（AST），兼容算术运算、比较表达式、条件分支（`IF...THEN...ELSE`）、子程序跳转（`GOSUB...RETURN`）与延时等待循环。
  - 虚拟执行机环境（VM）支持变量存储、按键等待（`CALL IRKEY`）、伪随机数生成（`CALL RAND`）与卡拉OK播放列表指令集（`KARAOKE`）。
  - 内置对历史遗留课件脚本语法笔误的容错与别名机制。
- **多媒体音视频播放**:
  - CD-XA 扇区解复用器（支持 RIFF CD-XA 与原始 PS 流解复用），用于提取 MPEG-1 视频流。
  - 桥接集成 `pl_mpeg`，支持 MPEG-1 视频帧解码与 MPEG-1 Layer II 音频解码。
  - 基于呈现时间戳（PTS）的音画同步、播放控制（播放/暂停）与拖动快进定位。
  - 背景音效循环播放（`BGSOUND`）与交互按键音效即时播放（`.WAV`）。
- **现代化用户界面**:
  - 基于 `egui` 与 `eframe` 构建的跨平台图形界面，支持高 DPI 缩放与等比例画面渲染。
  - 交互热区视觉高亮框与鼠标悬停/点击响应。
  - 浮动视频控制栏，包含播放/暂停按钮、进度条及播放时长统计。
  - 可折叠虚拟遥控器面板，支持物理键盘快捷键映射与虚拟遥控键码触发。
  - 支持光盘根目录选择器与未加载光盘占位图。
- **测试与持续集成**:
  - 包含内置独立的 Mock 测试夹具（`tests/fixtures/mock_disc`），脱离物理光驱即可 100% 离线运行完整测试套件。
  - 覆盖格式解析、脚本虚拟机、音频混合、视频流回放等 47 项集成与单元测试。
  - 完善的 GitHub Actions 持续集成自动化工作流，支持 Windows 与 Ubuntu 跨平台自动化测试。

[Unreleased]: https://github.com/NimitzDEV/vcd30player/compare/v0.1.6...HEAD
[0.1.6]: https://github.com/NimitzDEV/vcd30player/compare/v0.1.5...v0.1.6
[0.1.5]: https://github.com/NimitzDEV/vcd30player/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/NimitzDEV/vcd30player/compare/v0.1.0...v0.1.4
[0.1.0]: https://github.com/NimitzDEV/vcd30player/releases/tag/v0.1.0

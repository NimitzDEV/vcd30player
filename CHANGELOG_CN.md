# 更新日志 (Changelog)

本项目的所有重要变更均记录于此文件中。
文档格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，并遵循 [语义化版本规范](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]

## [0.4.0] - 2026-09-12

### 新增 (Added)
- 荧光绿 CRT 风格屏幕提示（OSD）图层，带四向暗色描边与渐隐动画。
- 三种 OSD 显示模式：按需自动隐藏、右上角常驻时间码与完全关闭。
- 屏幕操作反馈涵盖播放暂停、快进快退、切换曲目、声道切换、光盘格式识别与数字键输入。
- 底部状态栏新增 OSD 模式切换按钮，位于关于按钮前方。
- 全局快捷键：`O` 切换 OSD 模式，`L` 切换左右声道，`R` 切换循环模式。
- Windows 可执行文件属性中嵌入 PE 版本信息（产品名称、文件说明、版本号与版权）。
- Windows 生产版本配置图形界面子系统，启动时不再弹出控制台黑窗口。

### 变更 (Changed)
- 热区高亮开关与画面线框仅在 VCD 3.0 互动模式下展示和绘制。

### 修复 (Fixed)
- 修复遥控器输入当前正在播放的曲目号（如 38）时，画面与状态栏停留在输入提示态的问题。
- 修复 VCD 2.0 模式下输入未单独定义 PBC 播放列表的轨道号时无法切歌的问题。

## [0.3.1] - 2026-09-12

### 修复 (Fixed)
- **更新详情窗口尺寸与布局**:
  - 修复下载更新时进度条与取消按钮水平布局反馈导致窗口宽度异常扩大的问题。
  - 为更新详情窗口设置最大/最小宽度限制与不可变尺寸约束。
- **发布日期格式化**:
  - 优化发布日期展示，统一将 ISO 时间戳规范化为「年月日 时分秒」（`YYYY-MM-DD HH:MM:SS`）。

## [0.3.0] - 2026-09-12

### 新增 (Added)
- **软件版本检查与就地自更新**:
  - 基于 Cloudflare R2 / S3 兼容对象存储与 CDN 边缘分发，实现轻量级版本检查与安装包分发。
  - 「关于」对话框提供版本状态展示与检测入口，启动时保持静默离线，仅在呼出关于时按需请求。
  - 双语更新日志展示弹窗，自适应系统语言（中文/英文）默认切换。
  - 后台流式下载安装包，实时展示百分比与流量进度，支持中途安全取消。
  - SHA-256 完整性哈希校验，防止下载损坏或残缺文件。
  - 基于 `self-replace` 机制实现跨平台（Windows/Linux）运行中二进制文件原子替换与无缝重启。
- **CI 流水线自动化分发**:
  - 自动化构建产物归档、提取双语更新日志并生成 `version.json` 与 `checksums.txt`。
  - 同步上传至 Cloudflare R2 对象存储与 GitHub Releases 双通道。

## [0.2.0] - 2026-09-11

### 新增 (Added)
- **VCD 2.0 PBC (Playback Control) 状态机**:
  - 解析 `VCD/LOT.VCD`（位置查找表）与 `VCD/PSD.VCD`（播放序列描述表），支持白皮书规范中的 `PlayList`、`SelectionList` 与 `EndList` 结构。
  - 支持多级选单、曲目链表播放、延时等待与超时自动跳转。
  - 支持静态高分辨率菜单帧解码与渲染（`/SEGMENT/ITEMxxxx.DAT`）。
  - 支持 MPEG-1 动态交互式视频菜单回放。
  - 底部控制栏新增 PBC 按钮，可返回主选单。
- **播放模式识别与切换**:
  - 自动识别并切换 VCD 3.0 交互模式、VCD 2.0 PBC 模式与 VCD 1.0 线性视频模式。
  - 切换模式时清理并重建对应状态。
- **多位数字按键输入缓冲**:
  - 数字键输入缓冲（曲目数大于 9 时暂存，等待 `Enter`、第二位数字或 2.0 秒超时自动确认）。
  - 状态栏实时反馈输入状态（如 `输入曲目: 1 (按 Enter 确认或等待)`）。
  - 支持 VCD 1.0 模式下的数字键切歌。

### 修复 (Fixed)
- **曲目播放期间数字键误跳片头视频**:
  - 修复 VCD 2.0 模式播放曲目时按数字键误跳转至开场片头（LID 1）的问题；改为检索对应 PSD 描述符定位物理音轨，保留 PBC 返回逻辑。
- **曲目列表时间码格式**:
  - 曲目列表时间码分隔符由冒号 `:` 改为小数点 `.`（如 `[05:02.70]`），避免与时分秒格式混淆。
- **PBC 菜单状态残留**:
  - 进入曲目播放时清除标题栏与状态栏残留的 PBC 菜单名称。
- **主页按钮状态约束**:
  - 非 VCD 3.0 模式下禁用主页按钮。
- **侧面板关闭按钮字符渲染**:
  - 替换侧面板关闭按钮的字符，解决部分系统上渲染为方块的问题。

### 变更 (Changed)
- **侧面板展开窗口尺寸自适应**:
  - 展开侧栏时自动扩展窗口宽度，折叠时还原，保持画面比例。
- **底部控制栏布局调整**:
  - 将播放模式切换控件移至声道切换控件之后。
  - 将声道、播放顺序、关于等按钮精简为图标样式。
  - 重置按钮改为从头重新加载当前播放模式。
- **文档更新**:
  - 精简中英文 README 与 CHANGELOG，去除重复的功能列表与宣传性表述。

## [0.1.6] - 2026-09-10

### 新增 (Added)
- **自定义窗口标题栏**:
  - 基于 egui 渲染的自绘无边框窗口标题栏，高度与底部工具栏统一为 `26.0px`。
  - 居中展示窗口标题，支持标题栏区域拖拽移动窗口与双击最大化/还原切换。
  - 纯矢量绘制的窗口控制按钮（最小化、最大化/还原、关闭及悬停高亮），无需依赖外部字形字体。
  - 8 方向边缘与四角无边框拖拽缩放手柄。
  - 为无边框窗口增加 1px 外边框。
- **状态提示区域**:
  - 状态栏统一显示未加载引导、当前页面与视频流元数据、2 秒操作反馈以及悬停热区指示。

### 变更 (Changed)
- **底部控制栏布局与播放器控件**:
  - 底部控制面板重构为双行布局：第 1 行用于光盘加载管理与视频播放控制，第 2 行用于导航、调试开关与状态信息展示。
  - 播放控制按钮更新为图标控件（`▶`/`⏸`, `⏹`），支持悬停提示与快捷键（`Space`, `ESC`）。
  - 进度条自动拉伸填充剩余可用宽度，右侧显示已播放时间与总时长。
  - 统一底部面板上下边距与分割线间距为 `6.0px`。
  - 统一工具栏各元素与垂直分割线之间的水平间距为 `8.0px`。

### 修复 (Fixed)
- **交互脚本动画显示延时**:
  - 为 `DRAWIMAGE` 指令加入 300ms 延时机制，保证多帧动画（如射击判定、计分变化）与音效配合展示。
  - 在动画或延时执行期间禁用鼠标热区悬停与点击交互，防止动画被打断。
  - 优化画布纹理刷新逻辑，支持就地更新，避免不必要的 GPU 纹理重新分配。
- **热区悬停残留清理**:
  - 点击 DAT 视频跳转目标及视频播放期间，立即清除热区悬停高亮状态。
- **无头环境音频初始化异常**:
  - 无头 CI 环境（`CI=true` / `GITHUB_ACTIONS=true`）下跳过音频驱动初始化，避免无音频硬件时崩溃。

---

## [0.1.5] - 2026-09-08

### 新增 (Added)
- **复合页面覆盖层贴图支持**:
  - 支持解析复合 CHM 页面并将覆盖图层合成到底层画布（例如习题页面的答案补丁图 `AS011_*.YBM`）。
  - 在覆盖层文档未占用热区区域时，保留底层背景与基础页面的热区点击交互能力。
  - 页面导航跳转时自动清理覆盖层状态。
- **NTSC 视频流解码支持**:
  - 扩展 MPEG-1 视频解码器，除了标准的 PAL 制式（`352x288` @ 25.0 fps）外，支持 NTSC 分辨率（`352x240` @ ~29.97/30.0 fps）视频流解码与播放。

### 修复 (Fixed)
- **下划线热区点击容差优化**:
  - 为细窄下划线热区（如填空题横线）增加向上点击容差，允许点击横线上方的空白区域触发交互。
- **视频播放并发与 ESC 退出防死锁**:
  - 修复视频播放中按 Escape 键退出时的死锁问题，确保返回上一个 CHM 页面。

---

## [0.1.4] - 2026-09-07

### 新增 (Added)
- **动态文本变量与计分展示 (Chunk 14 `TEXT`)**:
  - 支持解析并渲染 Chunk 14 动态文本覆盖层到画布。
  - 实现变量实时格式化与计分器展示（例如互动游戏页面 `GAME051` 和 `SCORE05X` 中的得分统计）。
- **热区微脚本支持 (Chunk 13 `MICRO`)**:
  - 支持执行嵌入在热区记录中的单行微脚本（例如变量自增与赋值 `i_e=i_e+2`）。

### 修复 (Fixed)
- **片头视频播放条件约束**:
  - 仅在 `AUTORUN.CLS` 包含 `PlayMpeg` 或显式 `.DAT` 路径时播放片头视频，避免在无片头视频的光盘上播报无效音轨。
- **CHM 数据缓冲与脚本注释容错**:
  - 优先读取 CHM Chunk 3 偏移 `0x60` 处的定长前缀图像文件名，防止脏数据干扰。
  - 在拆分脚本指令时，保留 `REM` 与 `'` 注释内容中的冒号。
  - 异步后台脚本运行完毕后不自动跳转，保持交互页面处于打开状态。

### 变更 (Changed)
- 启动时默认折叠虚拟遥控器面板。
- 更新项目说明文档，补充预编译二进制安装与开发指南。

---

## [0.1.0] - 2026-09-06

### 新增 (Added)
- **光盘格式解析引擎**:
  - 支持解析光盘根目录类描述文件（`AUTORUN.CLS` / `ENREACH.CLS`）并定位启动首页。
  - 支持解析编译型 `<COMPHTML>` (.CHM) 页面，包括背景图像引用、页面导航跳转表与多边形/矩形按钮热区。
  - 支持解码 `<YUVBMP>` (.YBM) 8 位 YUV 调色板图像并转换为 RGBA。
- **VCDSCRIPT 脚本引擎**:
  - 支持词法分析与抽象语法树（AST），兼容算术运算、比较表达式、条件分支（`IF...THEN...ELSE`）、子程序跳转（`GOSUB...RETURN`）与延时等待循环。
  - 虚拟执行机环境（VM）支持变量存储、按键等待（`CALL IRKEY`）、伪随机数生成（`CALL RAND`）与卡拉OK播放列表指令集（`KARAOKE`）。
  - 内置对历史遗留课件脚本语法笔误的容错与别名机制。
- **多媒体音视频播放**:
  - CD-XA 扇区解复用器（支持 RIFF CD-XA 与原始 PS 流解复用），用于提取 MPEG-1 视频流。
  - 桥接集成 `pl_mpeg`，支持 MPEG-1 视频帧解码与 MPEG-1 Layer II 音频解码。
  - 基于呈现时间戳（PTS）的音画同步、播放控制（播放/暂停）与快进定位。
  - 背景音效循环播放（`BGSOUND`）与按键音效即时播放（`.WAV`）。
- **用户界面**:
  - 基于 `egui` 与 `eframe` 构建的跨平台图形界面，支持高 DPI 缩放与等比例画面渲染。
  - 交互热区视觉高亮框与鼠标悬停/点击响应。
  - 浮动视频控制栏，包含播放/暂停按钮、进度条及播放时长统计。
  - 可折叠虚拟遥控器面板，支持物理键盘快捷键映射与虚拟遥控键码触发。
  - 支持光盘根目录选择器与未加载光盘占位图。
- **测试与持续集成**:
  - 内置独立的 Mock 测试夹具（`tests/fixtures/mock_disc`），无需物理光驱即可运行全部测试。
  - 测试用例覆盖格式解析、脚本虚拟机、音频与视频播放。
  - 配置 GitHub Actions 工作流，支持 Windows 与 Ubuntu 自动化测试。

[Unreleased]: https://github.com/NimitzDEV/vcd30player/compare/v0.4.0...HEAD
[0.4.0]: https://github.com/NimitzDEV/vcd30player/compare/v0.3.1...v0.4.0
[0.3.1]: https://github.com/NimitzDEV/vcd30player/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/NimitzDEV/vcd30player/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/NimitzDEV/vcd30player/compare/v0.1.6...v0.2.0
[0.1.6]: https://github.com/NimitzDEV/vcd30player/compare/v0.1.5...v0.1.6
[0.1.5]: https://github.com/NimitzDEV/vcd30player/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/NimitzDEV/vcd30player/compare/v0.1.0...v0.1.4
[0.1.0]: https://github.com/NimitzDEV/vcd30player/releases/tag/v0.1.0

